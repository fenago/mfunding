# Copy to /opt/sms-bridge/.env on the droplet and fill in the REAL values.
# This file is a template only — never put real secrets in it, never commit .env.

# JMP / xmpp.chat account that owns the number +1 (786) 504-1159
XMPP_JID=mfunding@xmpp.chat
XMPP_PASSWORD=change-me

# Supabase project MFunding (ehibjeonqpqskhcvizow)
SUPABASE_URL=https://ehibjeonqpqskhcvizow.supabase.co
# Project Settings -> API -> service_role secret. Server-side only. Never ships to a browser.
SUPABASE_SERVICE_ROLE_KEY=change-me

# How often to poll sms_messages for status='queued' rows (ms)
POLL_MS=2000
